package server_test

import (
	"testing"
)

func Test_Server_Login(t *testing.T) {
	// assert.Equal(t, "tester", testUser.Login)
	// ctx := context.TODO()
	// conn, err := grpc.DialContext(ctx, "bufnet", grpc.WithContextDialer(bufDialer), grpc.WithInsecure())
	// if err != nil {
	// 	t.Fatalf("Failed to dial bufnet: %v", err)
	// }
	// defer conn.Close()

	// client := pb.NewSecurityServiceClient(conn)

	// tp, err := client.Login(ctx, &pb.LoginReq{
	// 	Login:    "admin",
	// 	Password: "12344321",
	// })
}
